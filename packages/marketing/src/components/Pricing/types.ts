export type tier = {
  title: string;
  subheader?: string;
  price: string;
  description: string[];
  buttonText: string;
  buttonVariant: string;
};
export type footer = {
  title: string;
  description: string[];
};
